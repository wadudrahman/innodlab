<div class="text-muted text-tiny mt-1">
    <small class="font-weight-normal">
        Today is {{ \Carbon\Carbon::now()->format('l') }}, {{ \Carbon\Carbon::now()->format('d F Y') }}
    </small>
</div>
